#include "ft_printf.h"

void ft_putnbr(int nb)
{
    char    c;
    int     sign;

    sign = 1;
    if (nb < 0)
    {
        sign = -1;
        ft_putchar('-');
    }
    if (nb / 10 )
        ft_putnbr(nb / 10);
    c = (nb % 10) + 48;
    ft_putchar(c);
}
